/**
 * 목업 시드가 **반드시 만들어 두어야 하는 상태**.
 *
 * 시드는 대부분 난수(seed 기반)로 뿌린다. 화면마다 여러 경우가 골고루 나와야
 * 만든 사람이 눈으로 확인할 수 있기 때문이다. 그런데 **포털에 처음 접속했을 때의
 * 그 한 사람**까지 난수에 맡기면, 오늘 근무가 없고 계약서는 전부 서명완료라
 * 화면은 멀쩡한데 눌러 볼 것이 하나도 없는 상태가 실제로 나온다.
 *
 * 그래서 이 사람 하나만 결과를 고정한다. 다른 상태(서류 반려 · 미제출 · 블랙리스트)는
 * 헤더의 스태프 전환기로 갈아 끼워 보면 된다.
 *
 * 서버가 붙으면 이 파일과 각 시드의 `데모 보정` 블록을 함께 지운다.
 */

/** 포털 기본 접속자. `useStaffSessionStore`의 초기값과 같아야 한다. */
export const DEMO_STAFF_ID = 1;
