# 관리자 API 베이스 URI
NEXT_PUBLIC_BASE_URI=http://localhost:8080

# enabled 일 때만 MSW 목업 워커가 뜬다. 서버에 붙일 때는 비운다.
NEXT_PUBLIC_API_MOCKING=enabled

# 하위 경로 배포용 접두사. 로컬에서는 비운다.
# 깃허브 페이지 워크플로가 /festival 을 넣어 준다. (.github/workflows/deploy-pages.yml)
# NEXT_PUBLIC_BASE_PATH=
